<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= @product.price %>
</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %> |

<%= link_to 'See Photos', product_photos_path(@product) %> |
<%= link_to 'Add Photo', new_product_photo_path(@product) %> |

<%= link_to 'See Reviews', product_reviews_path(@product) %> |
<%= link_to 'Add Review', new_product_review_path(@product) %>
