<!DOCTYPE html>
<html>
  <%= render 'layouts/rails_default' %>

  <body>
    <%= render 'layouts/header' %>

    <%= content_tag(:p, notice, class: "notice") %>
    <%= content_tag(:p, alert, class: "alert") %>

    <%= yield %>

    <!-- Restricts debug information to dev env -->
     <%= debug(params) if Rails.env.development? %>
  </body>
</html>
